# FastAPI and related
fastapi
uvicorn[standard]           # Includes uvloop, httptools, etc., for performance
python-multipart            # For handling form data/file uploads
aiofiles                    # Required for async file uploads
python-dotenv               # For managing .env config files
httpx                       # Async HTTP client

# PDF + text handling
PyPDF2
pypdf                       # Some loaders prefer this over PyPDF2

# LangChain (>= v0.2)
langchain>=0.2.0
langchain-community>=0.0.21  # Community loaders like PyPDFLoader
langchain-core>=0.1.34       # Core schema like Document
langchain-text-splitters>=0.0.1  # Text splitting utilities

# Vector DB + embedding models
chromadb
sentence-transformers
huggingface-hub

# LLM Provider (OpenAI)
openai
